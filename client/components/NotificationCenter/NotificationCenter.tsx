import classes from "./NotificationCenter.module.scss";

const NotificationCenter = () => {
  return (
    <aside className={classes.notificationsContainer}>
      <h1 style={{ margin: "auto" }}>Notification Center</h1>
    </aside>
  );
};

export default NotificationCenter;
